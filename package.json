{
  "name": "text-clock-card",
  "version": "1.0.0",
  "description": "Word clock card for Home Assistant Lovelace",
  "type": "module",
  "main": "dist/text-clock-card.js",
  "scripts": {
    "build": "rollup -c rollup.config.js",
    "watch": "rollup -c rollup.config.js --watch"
  },
  "devDependencies": {
    "rollup": "^4.0.0"
  }
}
